services:
  traefik:
    image: traefik:v2.11
    container_name: traefik
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"
      - "8080:8080" # Dashboard
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    networks:
      - app-network

  nginx:
    image: nginx:alpine
    container_name: secure-drop-nginx
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - .:/var/www:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.securedrop.rule=Host(`3.90.33.65`)"
      - "traefik.http.routers.securedrop.entrypoints=web"
      - "traefik.http.services.securedrop.loadbalancer.server.port=80"
    depends_on:
      - app
    networks:
      - app-network

  app:
    build: .
    container_name: secure-drop-app
    user: "1000:1000"
    environment:
      WWWUSER: ${WWWUSER:-1000}
      WWWGROUP: ${WWWGROUP:-1000}
      APP_KEY: ${APP_KEY}
      DB_HOST: db
      DB_DATABASE: secure_drop
      DB_USERNAME: root
      DB_PASSWORD: root
    volumes:
      - .:/var/www
    networks:
      - app-network

  db:
    image: mysql:8
    container_name: secure-drop-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: secure_drop
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  db-data:
